initSidebarItems({"struct":[["Component","A `Component` wraps a `Render` or `App` type to allow the runtime for fine-grained `render()` calls. This avoids re-rendering the whole DOM in each cycle."],["Updated","Allows the `update()` cycle of an application or component to signal the runtime what portion of the DOM requires re-rendering."]]});