initSidebarItems({"struct":[["Component","A `Component` wraps a `Render` or `App` type. A `Component` is mounted into a DOM and is tracked by the `Runtime`. This  allows the runtime to call `render()` only if re-render is requird. This avoids re-rendering the whole DOM in each cycle and reduces the size of the DOM to be diffed."],["Updated","Allows the `update()` cycle of an application or component to signal the runtime what portion of the DOM requires re-rendering."]]});